# Copy this file to ".env" and fill in your keys. Do NOT commit your real .env to version control.

# OpenAI API key (optional) — if set, server will use OpenAI for chat responses
# Example (Windows PowerShell):
# $env:OPENAI_API_KEY = "sk-..."
OPENAI_API_KEY=

# GROQ / Sanity API key (if you want to integrate Sanity later)
GROQ_API_KEY=
